{
  "name": "kanban-task-manager",
  "version": "1.0.0",
  "description": "Kanban Task Manager built with TypeScript - CRUD operations with LocalStorage persistence",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
